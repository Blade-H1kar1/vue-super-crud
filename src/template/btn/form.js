export default {
  // submit: (item, { ctx }) => ({
  //   label: "提交",
  //   icon: "el-icon-check",
  //   type: "primary",
  //   onClick: ctx.submit,
  // }),
  // reset: (item, { ctx }) => ({
  //   icon: "el-icon-refresh",
  //   label: "重置",
  //   onClick: ctx.reset,
  // }),
};
